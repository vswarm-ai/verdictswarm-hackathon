# VerdictSwarm environment example

# --- Base RPC ---
# Public Base RPC (safe default). Override in production.
BASE_RPC_URL=https://mainnet.base.org

# --- Token gating ($VSWARM) ---
# Token address will be filled after Virtuals listing.
VSWARM_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

# Optional: override for local/dev without chain calls
# VSWARM_MOCK_BALANCE=0

# Tier thresholds (token units, NOT wei)
VSWARM_TIER_1_THRESHOLD=1000
VSWARM_TIER_2_THRESHOLD=5000
