'use client';

// Backwards-compatible wrapper. Prefer CourtroomBackground.
import CourtroomBackground from './CourtroomBackground';

export default function BeeBackground() {
  return <CourtroomBackground />;
}
